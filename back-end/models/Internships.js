/*const mongoose = require("mongoose");

const InternshipsSchema = new mongoose.Schema([
    companyName: {
        type: String,
        maxlength: 100,
        minlength: 10.
    },

    companyAddress: {
        type: String,
        maxlength: 100,
        minlength: 10,

    },

]);*/
